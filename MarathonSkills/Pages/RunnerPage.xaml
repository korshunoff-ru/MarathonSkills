<local:BasePage x:Class="MarathonSkills.RunnerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarathonSkills"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Title="Стать бегуном">

    <Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">

            <Button Style="{StaticResource MenuButtonStyle}">

                <StackPanel>
                    <Viewbox Stretch="Uniform" Width="64" Height="64">
                        <ContentControl Content="{StaticResource RunnerIcon}"/>
                    </Viewbox>
                    <StackPanel Width="140"
                          Height="70"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource MenuItemStyle}">
                            ВХОД
                        </TextBlock>
                        <TextBlock Style="{StaticResource MenuItemDescriptionStyle}">
                            Если вы учавствовали ранее
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

            </Button>

            <Button Style="{StaticResource MenuButtonStyle}">

                <StackPanel>
                    <Viewbox Stretch="Uniform" Width="64" Height="64">
                        <ContentControl Content="{StaticResource MoneyIcon}"/>
                    </Viewbox>
                    <StackPanel Width="140"
                          Height="60"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource MenuItemStyle}">
                            РЕГИСТРАЦИЯ
                        </TextBlock>
                        <TextBlock Style="{StaticResource MenuItemDescriptionStyle}">
                            Если вы здесь впервые
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

            </Button>


        </StackPanel>

    </Grid>
</local:BasePage>
